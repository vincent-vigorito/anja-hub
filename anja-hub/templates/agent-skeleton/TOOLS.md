---
auto_generated: true
generator: anja/scripts/tools_md.py
agent: {AGENT_NAME}
updated: {DATE}
---

# Tools (agent-level)

<!--
  TOOLS.md di questo agent. Auto-generato — NON modificare manualmente.
  Riesegui via:
    python3 anja/scripts/tools_md.py --target <hub>/agents/{AGENT_NAME}

  Per default, l'agent eredita i tool del hub-level (`<hub>/.mcp.json`, `<hub>/TOOLS.md`).
  Se questo agent ha tool aggiuntivi/restrittivi, vivono in `<hub>/agents/{AGENT_NAME}/.mcp.json`
  o sono dichiarati in `config.json:allowed_tools`.
  Token budget HOT: ~200.
-->

## MCP servers (eredita hub + override)

(da popolare via tools_md.py)

## Skills disponibili

<elenca solo le skill rilevanti per questo agent. Sottoinsieme delle hub skills.>

## Tool preferiti

<elenca i tool che questo agent USA spontaneamente più spesso, anche se altri sono permessi.>

## Constraints

<eventuali restrizioni: tool VIETATI per questo agent (es. writer non può Bash).>
