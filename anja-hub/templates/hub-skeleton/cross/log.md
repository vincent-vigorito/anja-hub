---
title: Cross-project Log
type: log
created: "{{CREATED}}"
---

# Cross-project log

Append-only. Fixed prefix: `## [YYYY-MM-DD] type | description`.

Valid types: `hub-init`, `register`, `unregister`, `sync`, `cross-query`, `cross-rebuild`.

Regex pattern: `(\w[\w-]*)`.

---

## [{{CREATED}}] hub-init | hub initialized
