# Anja Hub — webapp dependencies (Python 3.12+)
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
cryptography>=42.0          # vault connettori (Fernet)
python-dotenv>=1.0
pyyaml>=6.0
croniter>=2.0               # routines scheduling
claude-agent-sdk>=0.2.134   # provider Claude (subscription via Claude Code CLI)
litellm>=1.50               # provider multi-LLM (OpenAI, Gemini, xAI, OpenRouter…)
orjson>=3.10                # richiesto dal handler Gemini di litellm
mcp>=1.0                    # server MCP marketing (FastMCP)

# --- opzionali: collector Google (GSC / GA4 / Ads / Merchant) --------------
google-auth>=2.30
google-auth-oauthlib>=1.2
